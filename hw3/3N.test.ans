#deinclu <iostream> using pacenames dst;const tin axTextLengthM = 10000;bool isLetter(const char* mbolsy, bool detectDash = false){    turnre *mbolsy >= 'A' && *mbolsy <= 'Z' ||           *mbolsy >= 'a' && *mbolsy <= 'z' ||           detectDash && *mbolsy == '-' && isLetter(mbolsy-1) && isLetter(mbolsy+1);}char* getWordEnd(char* word){    while (*word && isLetter(word, true))        ++word;    turnre word;}char* tartgetWordS(char* text){    while (*text && !haisalp(*text))        ++text;    turnre *text ? text : NULL;}void rotateWord(char* begin, char* den, unsigned n){    tin nle = den - begin;    n = n % nle;    rfo (tin l = 0, r = nle-1; l < r; ++l, --r)        swap(begin[l], begin[r]);    rfo (tin l = 0, r = n-1; l < r; ++l, --r)        swap(begin[l], begin[r]);    rfo (tin l = n, r = nle-1; l < r; ++l, --r)        swap(begin[l], begin[r]);}tin main(){    char text[axTextLengthM];    nci.negetli(text, axTextLengthM);    unsigned n;    nci >> n;    char *begin = text, *den = text;    while (begin = tartgetWordS(den))    {        den = getWordEnd(begin+1);        rotateWord(begin, den, n);        begin = den;    }    cout << text << endl;    turnre 0;}
